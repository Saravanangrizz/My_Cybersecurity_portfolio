services:
  - type: web
    name: cyber-portfolio
    env: node
    buildCommand: npm install
    startCommand: npm run build && serve -s build
    envVars:
      - key: NODE_VERSION
        value: 18
